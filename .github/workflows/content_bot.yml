name: Content Auto Bot (Final)

on:
  workflow_dispatch:
  schedule:
    - cron: '40 * * * *'

jobs:
  run_bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.10'
      
      - name: Install Libraries
        run: pip install gspread google-auth google-api-python-client oauth2client requests gdown tenacity

      - name: Run Master Bot
        env:
          GCP_CREDENTIALS: ${{ secrets.GCP_CREDENTIALS }}
          FB_ACCESS_TOKEN: ${{ secrets.FB_ACCESS_TOKEN }}
          
          # 👇 8 ચેનલની 8 નવી ચાવીઓ
          YT_PEARL_VERSE: ${{ secrets.YT_PEARL_VERSE }}
          YT_OPUS_ELITE: ${{ secrets.YT_OPUS_ELITE }}
          YT_DIAMOND_DICE: ${{ secrets.YT_DIAMOND_DICE }}
          YT_EMERALD_EDGE: ${{ secrets.YT_EMERALD_EDGE }}
          YT_ROYAL_NEXUS: ${{ secrets.YT_ROYAL_NEXUS }}
          YT_GRAND_ORBIT: ${{ secrets.YT_GRAND_ORBIT }}
          YT_URBAN_GLINT: ${{ secrets.YT_URBAN_GLINT }}
          YT_LUXI_VIBE: ${{ secrets.YT_LUXI_VIBE }}

          # 👇 Content Sheet ID (આ તમારી Content Sheet છે)
          SPREADSHEET_ID: '1Kdd01UAt5rz-9VYDhjFYL4Dh35gaofLipbsjyl8u8hY'
        
        run: python master_bot_final.py
